# golang

1. A simple command line to-do list application. It allows you to create, view, update, and delete tasks with persistent storage using a local JSON file.

Features:
- Add new to-do items via command line flags
- View the complete list of to-dos with their statuses
- Update task descriptions and statuses
- Delete items by ID
- Automatically saves to and loads from disk
